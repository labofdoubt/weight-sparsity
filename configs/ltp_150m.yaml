# ~152M parameters, LTP sparsity on the MLP weights.
_base_:
  - model/large.yaml
  - train/default.yaml
  - sparsity/ltp.yaml

train:
  run_name: ltp_150m
  batch_size: 128
  micro_batch_size: 16
  max_steps: 40000
  lr: 4.0e-4

sparsity:
  beta_warmup_steps: 2000
