# abs_topk + one-sided entropy calibration, exact LapSum gradient weights
_base_: base.yaml

activation_bottleneck:
  selection_mode: abs_topk
  boundary_mode: outside_only
  one_sided_weight_mode: true_gradient
  effective_count_metric: entropy
